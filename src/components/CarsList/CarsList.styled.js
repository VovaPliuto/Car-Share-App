import { styled } from 'styled-components';

const StyledCarsList = styled.ul`
  display: flex;
  flex-wrap: wrap;
  column-gap: 29px;
  row-gap: 50px;
  margin: 0;
  padding: 0;
  list-style: none;
`;

export { StyledCarsList };
